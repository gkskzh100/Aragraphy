<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sk.mendolong.aragraphy" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Hstory_Activity"></activity>
        <activity android:name=".Invitefriends_Activity"></activity>
        <activity android:name=".Store_Activity"></activity>
        <activity android:name=".Myleaf_Activity"></activity>
        <activity android:name=".Popular_Activity"></activity>
        <activity android:name=".Write_Activity"></activity>
        <activity android:name=".StartActivity"></activity>
    </application>

</manifest>
